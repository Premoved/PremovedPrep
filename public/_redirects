# Retired player pages: 301 to the search page. Non-canonical slugs get a second redirect from
# functions/search/opponent/[slug].ts. Must precede the catch-all (first match wins).
/player/*            /search/opponent/:splat  301
/players             /search                  301
/sitemap-players.xml /sitemap-opponents.xml   301

# Old Desktop Agent page.
/agent               /app                     301

# SPA fallback.
/*  /index.html  200
